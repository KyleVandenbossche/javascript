// Add
let x = y + 3;

// Subtract
let x = y - 7;

//Multiply
let total = price * 5;

//Divide
let sandwiches = slices / 2;

//Modulus (remainer)
let extra = 13%5;

//Concatenate (Strings)
let title = "Sir" + name;



/* Assignment operators

'='  Set value
'+=' Add or concatentate and set
'-=' Subtract and set 
'*=' Multiply and set
'/=' Divide and set
'%=' Modulus and set


'++' Increment (Set value + 1) count++;
'--' Decrement (Set value - 1) remaining--;


Comparison Operators
'===' Equal to (num === 69)
'!==' Not equal (option !== "quit")
'<' Less than (score < 10)
'>' Greater than (price >10)
'<=' Less or Equal (temperature <=32)
'>=' Greater or Equal (age >= 21)

Equals with Coercion (double equals)
3 === "3" // false
0 === false // false

3 == "3" // true
0 == false // true - interesting
true == "true" // false

Logical Operators
'&&' And // year >= 1900 && year <2000
'||' Or // day === "Sat" || day === "Sun"
'!' Not // !solutionFound, !(isWeekend || isHoliday)

